{
  "name": "concentration",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prepare": "git config core.hooksPath .git-hooks || echo 'Not in a git repo'",
    "install": "npm run prepare & npm ci",
    "dev": "vite",
    "stylelint": "stylelint 'src/**/*.css' --fix",
    "format": "prettier --write .",
    "build": "vite build",
    "preview": "vite preview"
  },
  "browserslist": ">1% and last 2 versions, not dead",
  "engines": {
    "node": ">=20.18.0"
  },
  "devDependencies": {
    "glob": "^11.0.1",
    "lint-staged": "^15.4.3",
    "postcss-preset-env": "^10.1.3",
    "prettier": "^3.4.2",
    "stylelint": "^16.14.1",
    "stylelint-config-standard": "^37.0.0",
    "vite": "^6.0.1",
    "vite-plugin-purgecss": "^0.2.12"
  }
}
